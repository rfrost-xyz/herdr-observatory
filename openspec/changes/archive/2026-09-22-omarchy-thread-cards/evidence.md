# Evidence

- Runtime `c628ff5` replaces the full-display terminal with persistent DOM header, labelled fleet strip, eight prominent thread cards and four recent observations. `viewModel` preserves Herdr states, native pane identifiers, explicit unknowns and supported hook information. Tests cover paging/total counts, hook/state separation, source loss, metadata expiry and DOM text safety.
- Observed state/hook sequences drive decaying card impulses and header pixels. Duplicates, quiet, paused, reduced, hidden and stale conditions are covered. The only effect canvas belongs to the recent-activity area. The unchanged one-line wrapper and all 37 exact final frames retain complete playback, non-repeat, cooldown and asynchronous cancellation tests.
- Synced palette variables colour surfaces, borders, text and effects. Browser visual QA used synthetic data at 1280x720 and 1920x1080, including three sources, multiple pages, long titles, dark/light palettes and offline state. Document dimensions matched viewport; card scroll heights matched client heights, native identities remained readable and no card contained a canvas. The 1080p density issue found during QA was fixed and rechecked.
- Required gates passed: 54 Python tests, 81 Node tests, JavaScript syntax, strict OpenSpec and diff checks. Python tests also passed inside the production image. Independent review approved after restoring the visible total count and correcting outdated README descriptions.
- Both iapetus Personal and ws-255 Work containers are healthy on `c628ff5`. Served app.js, style.css, effects.mjs and index.html match committed source on both. Both configured sources available and Work disclosure checked. Existing hooks/theme transport and startup remain unchanged.
- Retained `6ad74b4` and matching previous environment for rollback. Removed older application images, synthetic fixture servers/files, isolated browser profiles and test caches. Browser viewport override reset and test/reference tabs closed. No physical office-monitor interaction claimed.
