# Verification evidence

- Dashboard fullscreen: `web/index.html` provides a separate header icon; `web/app.js` routes F to the same action and maintains distinct observation focus. `tests/test_ui.cjs` covers switching, repeat actions and browser exit. Trusted Chromium clicks confirmed dashboard fullscreen, observation expansion and restoration, then F exit.
- Thread footer: both renderers show ordered host, pane, harness, model and known compactions on one line. Source ages and coverage remain in the accessible card description and instrument details. The former visual age row is gone. Chromium at 1280 × 720 measured a 14px one-line footer with eight cards and no page or thread scroll; 1280 × 800, 1920 × 1080 and 1920 × 1200 also fit.
- Weekly allowance: both renderers omit the visible weekly-left label and show signed percentage-point variance beside the remaining percentage. `web/allowances.mjs` retains the authoritative percentage and only computes pace with a fresh, valid future reset. `tests/test_allowances.mjs` verifies reserve, deficit, unknown values and fallback markup. Chromium rendered +7% for a 97% balance with about six days remaining.
- `npm run build:web`, the JavaScript suite, 122 Python tests, strict change validation, `node --check web/app.js` and `git diff --check` passed. The first sandboxed Python run could not bind Unix sockets; the rerun with socket access passed.
- Independent review found an inconsistency between small signed variance values and the accessible “On pace” label, including negative zero. The display value, colour and label now derive from the same rounded difference. A renewed review found no remaining actionable finding.
