# Dashboard display controls

The header needs its own fullscreen control while the observation focus remains independent. The thread footer currently wastes a second line on abbreviated ages, and the weekly card repeats its meaning instead of showing pace variance beside the remaining percentage.

This change adds a dashboard fullscreen icon and F shortcut, keeps observation focus on its separate control, moves compactions onto the metadata line, removes the visual age row, and puts signed pace variance beside the weekly percentage.
