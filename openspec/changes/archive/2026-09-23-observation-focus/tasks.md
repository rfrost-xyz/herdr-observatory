# Tasks

## 1. Thread card hierarchy

- [x] 1.1 Render host, Herdr pane, harness and model on one footer line in both renderers; verify ordered metadata and 1280px geometry.
- [x] 1.2 Put activity and tool on a full-width strip beneath the project and state row; verify DOM order and compact screenshots.

## 2. Observation history

- [x] 2.1 Render the bounded history in a scrollable container while keeping recent-only effect eligibility; verify UI tests and a browser mouse-wheel interaction.
- [x] 2.2 Replace the Fullscreen word with an accessible icon and expand observations beneath the persistent header; verify toggle tests and browser geometry at supported sizes.

## 3. Release gates

- [x] 3.1 Run the full test suites, strict OpenSpec validation and independent review; record evidence before archiving.
