## Source ages
The hook sequence dates the latest activity. The usage sequence dates Codex numeric usage and Pi last-response counters. Pi context and cumulative totals are recomputed by the extension for each hook, so their tile source is the hook. Each tile receives an independent last-known flag. The footer states both ages. Historical phase labels use past-observation wording; Herdr's state badge remains authoritative.

## Cache balance
Show the percentage as cache-read tokens divided by total input tokens only when all reported components reconcile and total input is positive. Retain compact read and uncached counts. When cache-write input is positive, add a separate labelled count on the same line and a third bar segment. Known counts remain visible even when other components are missing; inconsistent and zero-input balances have no percentage. Keep exact counts in tooltip and accessible detail.

## Layout and evidence
Raise small card label sizes in the 720p rules while preserving the eight-card grid and no document scrolling. Let the separate freshness labels wrap; shorten historical activity and keep cache counts on one line to limit vertical growth. Verify fixtures at both target dimensions where browser access is available; otherwise record the visual check boundary without claiming layout acceptance. Test source-specific ages, historical wording and cache segment conservation.
