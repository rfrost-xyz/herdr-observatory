# Tasks

## 1. Reporting and installation
- [x] 1.1 Implement bounded image reporter and native session guards; verify socket contract, expiry, malformed and out-of-order tests.
- [x] 1.2 Bundle Codex/Pi adapters and idempotent installer/uninstaller; verify preservation, conflicts, failure and payload tests.

## 2. Observation and display
- [x] 2.1 Extend probe and feed typed allowlists; verify Personal/Work, expiry and session binding tests.
- [x] 2.2 Render sampled technical details and notable CLI updates; verify UI regression and telemetry tests.

## 3. Delivery
- [x] 3.1 Document architecture, field coverage, setup, activation and removal; pass all repository gates and independent review.
- [ ] 3.2 Deploy and install on both authorised hosts; verify health, actual metadata round trip, installed hooks and retained rollback, then remove task artifacts and superseded image.
- [ ] 3.3 Reconcile evidence and clean final review, synchronise/archive specs and publish the ready PR with passing checks.
